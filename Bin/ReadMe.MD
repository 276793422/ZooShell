目录：
Plugins		目录放插件
Functions	目录放一些lua 脚本，解释器启动会自动加载，功能类似于加载内部函数，内部插件后缀为 .plug
Dll		目录放一些可能需要的DLL
API		目录放API 函数数据库，加载数据库之后可以直接使用WindowsAPI函数，类似C语言，连lib都不用导入



功能：
打开程序输入 ? 有帮助



完全兼容lua 语法，
大多数功能我都用过，
少部分没用过，
部分功能可能没有那么多用处，
支持lua 的大部分内置模块。
可能会有BUG ，但是大多数情况下不影响使用，
软件持续更新中。